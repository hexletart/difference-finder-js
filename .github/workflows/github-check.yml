name: github-check

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]

    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
      - name: npm install
        run: |
          make install
        env:
          CI: true
      - name: Run Linter
        run: |
          make lint
      - name: Run Tests
        run: |
          npm test
      - name: Test & publish code coverage
        uses: paambaati/codeclimate-action@v3.0.0
        env:
          CODECLIMATE_ID: ${{secrets.351e2684e4f251e31096c9f846d42e66646a07b0c0cd135895f7eeb7cd6eacbc}}
        with:
          coverageCommand: make test-coverage
          debug: true
